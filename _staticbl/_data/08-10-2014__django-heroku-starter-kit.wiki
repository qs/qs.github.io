title: Django Heroku starter kit
tags: python, django
date: 08-10-2014 02:37

Placeholder

Здесь будет инструкция, как сделать следующий набор штуковин работающим вместе:
* Heroku
* Django
* MongoDB
* python-social-auth

Работающих актуальных гайдов на данный момент нет.

Базовая установка с PostgreSQL:

pip install django-toolbelt
django-admin.py startproject project .

Конфиги:
echo "web: gunicorn project.wsgi --log-file -" > Procfile
pip freeze > requirements.txt

vim .gitignore
venv
*.pyc
staticfiles


vim wsgi.py
from django.core.wsgi import get_wsgi_application
from dj_static import Cling

application = Cling(get_wsgi_application())

git init
git remote add heroku git@heroku.com:morning-eyrie-9355.git
git add --all
git commit -m 'init'
git push heroku master

===Mongo
heroku addons:add mongohq:sandbox
heroku config| grep "MONGOHQ"



http://habrahabr.ru/post/160191/
https://github.com/MongoEngine/django-mongoengine
http://stackoverflow.com/questions/22861077/django-auth-with-mongoengine-db